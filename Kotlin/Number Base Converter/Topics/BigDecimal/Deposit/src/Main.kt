import java.lang.Math.pow
import java.math.BigDecimal
import java.math.RoundingMode

/*fun main() {
    val startingAmount = readln()
    val rate = readln()
    val years = readln()
    val res =
        (BigDecimal(startingAmount) * (BigDecimal(1) + (BigDecimal(rate).setScale(2) / BigDecimal(100))).pow(years.toInt())).setScale(
            2,
            RoundingMode.CEILING
        )
    println("Amount of money in the account: $res")
}*/

fun main() {
    val number = 1.1
    val a = number.toB

}


